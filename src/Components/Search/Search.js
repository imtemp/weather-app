function searchLocation() {
  // Change location in API url
}

const Search = () => {
  return (
    <div>
      <input type="text"></input>
      <button onClick={searchLocation}></button>
    </div>
  );
};

export default Search;
